import { runFunction } from '../runFunction';

export default {
  title: '@pro-utils/functions/runFunction',
};

export const runFunction = () => {
  const functionName = 'runFunction';
  return `Result: ${functionName}`;
};
